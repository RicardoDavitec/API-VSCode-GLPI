#!/usr/bin/env bash
# atualizar-kit — entrypoint bash → atualizar_kit.py
set -euo pipefail
KIT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec python3 "$KIT_ROOT/scripts/atualizar_kit.py" --kit="$KIT_ROOT" "$@"
