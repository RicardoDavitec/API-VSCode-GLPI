#!/usr/bin/env bash
# Wrapper direto — aplica JSON do retro-scan (pai→filho). Dry-run sem --apply.
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../../.." && pwd)"
exec "$ROOT/tools/glpi/glpi" retro-apply "$@"
