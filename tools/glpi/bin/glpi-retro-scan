#!/usr/bin/env bash
# Wrapper direto — sem skill. Executa levantamento S/P.
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../../.." && pwd)"
exec "$ROOT/tools/glpi/glpi" retro-scan "$@"
